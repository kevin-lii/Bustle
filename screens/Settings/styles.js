import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1,
    padding: 3,
    marginBottom: 10,
  },
  redirect: {
    justifyContent: "center",
    margin: 10,
    fontWeight: "bold",
    fontSize: 18,
    alignContent: "center",
    textAlign: "center",
  },
});
