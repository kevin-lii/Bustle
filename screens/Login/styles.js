import { StyleSheet } from "react-native";

const bigEdge = 60;
const smallEdge = 50;

export default StyleSheet.create({
  container: {
    display: "flex",
    marginLeft: 20,
    marginRight: 20,
  },
  button: {
    margin: 5,
  },
});
